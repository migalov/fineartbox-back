'use strict';

/**
 * filter-construction service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::filter-construction.filter-construction');
