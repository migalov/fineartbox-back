'use strict';

/**
 * filter-construction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::filter-construction.filter-construction');
